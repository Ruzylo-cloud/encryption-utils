# Encryption Utils

AES/RSA encryption library. Secure Ferrow agent data handling.

```javascript
const crypto = new EncryptionUtils(secret);
const encrypted = crypto.encrypt(sensitiveData);
```

Features: AES-256, RSA, key rotation, Ferrow security.
License: MIT
